export type VenueType = {
  id: string;
  name: string;
  address: string;
  description: string;
  image_url: string;
  latitude: number;
  longitude: number;
  owner_id: string;
  created_at: string;
}; 